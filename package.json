{
  "name": "what-the-wether",
  "version": "13.0.0",
  "private": true,
  "description": "What the Wether — a neon weather app with a real-map radar and a local roast AI. No API keys.",
  "scripts": {
    "test": "node tests/run.js",
    "serve": "python3 -m http.server 8000",
    "postinstall": "echo 'Run: npx playwright install chromium'"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  },
  "license": "MIT"
}
